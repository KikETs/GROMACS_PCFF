# TP1.5 Cut-off Regression Audit Runner

This runner stays inside TP1.5 scope:

- localize the cut-off-only nonbonded path used by TP1.3
- rerun a minimal exclusion-sensitive cut-off fixture
- rerun a minimal shift-sensitive periodic cut-off fixture
- preserve raw commands, provenance, and machine-readable summaries

Run with:

```bash
python3 tools/run_tp1_5_cutoff_audit/run_cutoff_audit.py
```
