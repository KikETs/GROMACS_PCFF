# M11.3 - M5 Chemistry-Scope Expansion

## Later M2 Update

The dense NPT parity blocker for the M5-derived dense scaffold is no longer universally true. M11.4 later promotes `monoglyme_ethane_litfsi_1to1_dense18` into a strict paired dense artifact and closes a `250 bar`, 100 ps target / final 50 ps density-volume parity gate for that explicit scaffold.

This later M2 result does not change the M11.3 scope: M11.3 itself remains a workflow-level chemistry expansion and GROMACS smoke report, not a dense-parity or transport-readiness report.

## Scope

This report closes M5 only for one explicit chemistry-scope expansion:

- system: `monoglyme_ethane_litfsi_1to1`
- added component family: `acyclic_alkane`
- added role: `neutral_additive`
- charged context: monoglyme + ethane + one Li/TFSI pair

It does not claim:

- broad PCFF chemistry coverage
- arbitrary alkane or neutral co-solvent support
- dense charged ensemble parity for the new assembly
- LAMMPS-vs-GROMACS transport parity
- charged transport readiness

## Artifact Inventory

Primary artifacts:

- [M5 chemistry expansion report](../tests/reference_results/pcff_charged_expansion/m5_monoglyme_ethane_litfsi_1to1/m5_chemistry_expansion_report.json)
- [M5 chemistry scope manifest](../tests/reference_results/pcff_charged_expansion/m5_monoglyme_ethane_litfsi_1to1/m5_chemistry_scope_manifest.json)
- [M5 SHA256 manifest](../tests/reference_results/pcff_charged_expansion/m5_monoglyme_ethane_litfsi_1to1/sha256_manifest.txt)
- [M5 source spec](../testdata/polymer_workflow_m5/cases/monoglyme_ethane_litfsi_1to1/spec.json)

Generated bundle:

- [Generated GROMACS topology bundle](../tests/reference_results/pcff_charged_expansion/m5_monoglyme_ethane_litfsi_1to1/generated_gromacs)
- [GROMACS smoke bundle](../tests/reference_results/pcff_charged_expansion/m5_monoglyme_ethane_litfsi_1to1/gromacs_smoke)

Runner:

- [M5 runner](../tools/run_pcff_charged_m5/run_m5_chemistry_expansion.py)

## Protocol

The M5 candidate is generated by the repository `polymer_workflow` path, not ACPYPE/GAFF2.

Validation gates:

- dry-run workflow rendering
- written topology bundle generation
- validate-existing re-render check
- charge-neutral Li/TFSI assembly check
- new `neutral_additive` role classified as `acyclic_alkane`
- GROMACS `grompp` smoke on generated topology and generated coordinates
- GROMACS run-0 rerun smoke on the same generated topology
- `grompp` is run without `-maxwarn`; any `grompp` warning fails the M5 smoke gate

## Results

M5 report status: `PASS`.

Assembly:

- total charge: `0.0`
- cation count: `1`
- anion count: `1`
- polymer component: `MONOGLY`
- neutral additive component: `ETHANE`

Family counts:

- `acyclic_polyether_oligomer`: `1`
- `acyclic_alkane`: `1`
- `lithium_cation`: `1`
- `tfsi_like_sulfonimide`: `1`

GROMACS smoke:

- `grompp_returncode`: `0`
- `grompp_warning_count`: `0`
- `mdrun_returncode`: `0`
- smoke status: `PASS`
- GROMACS binary: `../build/bin/gmx`

## Capability Delta

Previous chemistry boundary:

- PT8 glyme + Li/TFSI SPE cases
- small charged fixtures
- one dense-salt-polymer-derived strict subset

New M5 boundary:

- one additional workflow-level charged assembly with an acyclic alkane neutral additive: `monoglyme_ethane_litfsi_1to1`

This is a real chemistry-scope expansion, but it is narrow. It adds one validated component family/role combination, not broad PCFF chemistry.

## Verdicts

Claim honesty verdict: `PASS`.

Capability expansion verdict: `PASS` for M5 on this explicit workflow-level chemistry subset.

Broad chemistry verdict: `FAIL`.

Remaining blockers:

- no ambient 1 bar dense NPT parity for the new M5 chemistry
- no dense NPT parity for the new M5 chemistry outside the later explicit M11.4 `250 bar` dense18 campaign
- no strict LAMMPS-vs-GROMACS mechanical parity for the new M5 chemistry
- no transport-facing validation for the new M5 chemistry
- no broad alkane or arbitrary neutral-additive coverage
